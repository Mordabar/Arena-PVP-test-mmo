#!/usr/bin/env node
'use strict';const fs=require('fs'),path=require('path'),cp=require('child_process'),R=path.join(__dirname,'..');let files=[];for(const base of ['js','tools']){(function w(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory())w(p);else if(/\.js$/.test(e.name))files.push(p);}})(path.join(R,base));}let bad=[];for(const f of files){const r=cp.spawnSync(process.execPath,['--check',f],{encoding:'utf8'});if(r.status!==0)bad.push({f,err:r.stderr});}console.log((bad.length?'SYNTAX_V034: RECHAZADO':'SYNTAX_V034: APROBADO')+' · '+(files.length-bad.length)+'/'+files.length);if(bad.length)bad.forEach(x=>console.error(x.f+'\n'+x.err));process.exit(bad.length?1:0);
