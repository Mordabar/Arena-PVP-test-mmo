#!/usr/bin/env node
'use strict';const fs=require('fs'),path=require('path'),R=path.join(__dirname,'../..');let f=0;function g(x,n){console.log((x?'✓ ':'✗ ')+n);if(!x)f++;}const d=fs.readFileSync(path.join(R,'docs/ANIMATION_REFERENCE_V015.md'),'utf8'),c=fs.readFileSync(path.join(R,'js/render/skinnedAnimationContract.js'),'utf8');g(/báculo|staff/i.test(d)&&/arco|bow/i.test(d),'documento registra las dos familias de referencia');g(/RAISE.*DRAW.*RELEASE/is.test(d),'secuencia del arquero documentada');g(/PREPARE.*GATHER.*CHANNEL.*RELEASE/is.test(d),'secuencia del caster documentada');g(/casterCast/.test(c)&&/archerShot/.test(c)&&/meleeAction/.test(c),'contrato implementa caster/archer/melee');process.exit(f?1:0);
